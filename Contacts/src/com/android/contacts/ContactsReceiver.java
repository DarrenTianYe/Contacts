/*package com.android.contacts;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.util.Log;

//import com.neolink.contacts.data.PrivateContactsImportService;

public class ContactsReceiver extends BroadcastReceiver {
	private static final String LOG_TAG = "ContactsReceiver";

	@Override
	public void onReceive(Context context, Intent intent) {
		String action = intent.getAction();
		Log.d(LOG_TAG, "onReceive action:" + action);
//		if (action.equals(Intent.ACTION_BOOT_COMPLETED)) {
//			//intent.setClass(context, ContactsImportService.class);
//			//context.startService(intent);
//		} else if ("com.neolink.modechange".equals(action)) {
//			intent.setClass(context, ContactsImportService.class);
//			context.startService(intent);
//		} else if ("com.neolink.action.dynamic_group".equals(action)) {
//			intent.setClass(context, ContactsImportService.class);
//			context.startService(intent);
//		}
	}
}
*/